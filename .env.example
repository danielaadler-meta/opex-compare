DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=opex_compare
JWT_SECRET=change-me-to-a-random-secret
JWT_EXPIRATION=8h

# Hive connection (optional - Hive endpoints will gracefully degrade if not configured)
HIVE_HOST=localhost
HIVE_PORT=10000
HIVE_USERNAME=
HIVE_AUTH_MECHANISM=NOSASL
